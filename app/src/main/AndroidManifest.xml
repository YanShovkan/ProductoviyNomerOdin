<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.productoviynomerodin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/korzina"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProductoviyNomerOdin">
        <activity
            android:name=".activities.CardActivity"
            android:exported="true" />
        <activity
            android:name=".activities.OrderActivity"
            android:exported="true" />
        <activity
            android:name=".activities.DiscountCatalogActivity"
            android:exported="true"
            android:label="@string/activity_discount_catalog_label" />
        <activity
            android:name=".activities.CatalogActivity"
            android:exported="true"
            android:label="@string/activity_catalog_label" />
        <activity
            android:name=".activities.MapActivity"
            android:exported="true"
            android:label="@string/title_activity_map" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true" />
        <activity
            android:name=".activities.EnterActivity"
            android:exported="true"
            android:label="@string/activity_enter_label">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.RegisterActivity"
            android:exported="true"
            android:label="@string/activity_register_label" />
        <activity
            android:name=".activities.ProfileActivity"
            android:exported="true" />
    </application>

</manifest>